variables:
  MAVEN_CLI_OPTS: "-s .m2/settings.xml --batch-mode"
  MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"

stages:
    - test
    - build
    - deploy

test:
    stage: test
    script: "mvn test"

build:
    stage: build
    script: "mvn clean install"
    #only:
        #- main

deploy:
    stage: deploy
    script: "mv target/valit-backend.jar /db/valit-backend/feedback-backend/target"
    #only:
        #- main
    
